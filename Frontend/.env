# Vite environment variables for the front‑end. Copy this file to `.env` and
# adjust the values as needed. Variables prefixed with `VITE_` are exposed
# to your client‑side code. See http://vitejs.dev/guide/env-and-mode.html

# Base URL of the API server. During development with `npm run dev` in
# the root directory, this should match the backend PORT (e.g. 5000).
VITE_API_BASE=http://localhost:5000

# Title of the application. This can be referenced in your Vue code via
# import.meta.env.VITE_APP_TITLE. It is not currently used in the code
# provided but can be useful for future enhancements.
VITE_APP_TITLE=Employee Welfare System